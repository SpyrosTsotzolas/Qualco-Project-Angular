import { CountryStatsFilter } from './country-stats-filter';

describe('CountryStatsFilter', () => {
  it('should create an instance', () => {
    expect(new CountryStatsFilter()).toBeTruthy();
  });
});
